package com.javarush.test;


import java.util.*;


public class Solution {
    static int x;
    static int y;

    public static void main(String[] args) {
    /*    Solution solution = new Solution();
        Scanner scanner = new Scanner(System.in);
        String expression = new String()
                char[] chars = new char[]
     //   String expression = String.valueOf(scanner.nextInt());
     //   int result = Integer.parseInt(expression);
     //   System.out.println(expression);
      /*  x = scanner.nextInt();
        String symbol = scanner.next();
        y = scanner.nextInt();

        if (symbol.equals("+")) {
            System.out.print(solution.calculateArabicPlus(x, y));
        } if (symbol.equals("-")) {
            System.out.print(solution.calculateArabicMinus(x, y));
        } if (symbol.equals("*")) {
            System.out.print(solution.calculateArabicMultiplication(x, y));
        } if (symbol.equals("/")) {
            System.out.print(solution.calculateArabicDivision(x, y));
     //   } else {
      //      System.out.println("enter PLUS!!!");
        }



        scanner.close();
    }
    int calculateArabicPlus(int x1, int y1) {
        return x1 + y1;
    }
    int calculateArabicMinus(int x1, int y1) {
        return x1 - y1;
    }
    int calculateArabicMultiplication(int x1, int y1) {
        return x1 * y1;
    }
    int calculateArabicDivision(int x1, int y1) {
        return x1 / y1;
    }*/
    }
}




